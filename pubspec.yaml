name: wacken_lineup
description: Open Source Wacken Line-Up Viewer
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"
dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.4
  path_provider: ^2.0.11
  provider: ^6.0.3
  connectivity_plus: ^2.3.6


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0


flutter:

  uses-material-design: true

  assets:
    - assets/

